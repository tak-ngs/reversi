import { StoneColorPipe } from './stone-color.pipe';

describe('StoneColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StoneColorPipe();
    expect(pipe).toBeTruthy();
  });
});
